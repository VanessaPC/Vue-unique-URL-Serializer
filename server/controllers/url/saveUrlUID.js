import { Url } from "../../models/url.model";

/**
 * This method will create an Id,
 * will check whether it exists in the db
 * and regenerate it if it's positive, otherwise,
 * save it
 */

export const saveUrlUID = async (req, res) => {};
