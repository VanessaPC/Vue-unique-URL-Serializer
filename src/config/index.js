import { config as devConfig } from "./dev";
// import { config as prodConfig } from "./prod";

/**
 * ! todo:  ENV var not set properly to dev
 */

// export const config =
//   process.env.NODE_ENV === "production" ? prodConfig : devConfig;

export default devConfig;
